{
  "name": "otp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --no-source-map --style=compressed src/scss:build/css",
    "js": "uglifyjs src/js/otp.js -o build/js/otp.min.js -c",
    "build": "npm run sass && npm run js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.77.8",
    "uglify-js": "^3.19.0"
  }
}
